# Project Rules & Constraints

## Browser Restrictions
- **Do NOT open the browser**: Never launch browser tools or `browser_subagent` without explicit user permission in the request.
- Use terminal commands (`tsc`, `curl`) and static analysis for validation.
