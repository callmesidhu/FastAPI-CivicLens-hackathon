# Project Instructions & Agent Guidelines

## Critical Tool Rules
- **NEVER OPEN THE BROWSER**: Do not use `browser_subagent` or any browser interaction tools unless the user explicitly and directly asks you to open the browser.
- Perform all verification and testing strictly using CLI tools (e.g., `npx tsc --noEmit`, `curl`, automated test suites) and code inspection.
